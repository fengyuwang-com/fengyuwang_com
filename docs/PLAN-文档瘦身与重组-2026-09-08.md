# PLAN — 文档瘦身与重组（2026-09-08）

> 状态：已过独立评审，待执行（approve-with-changes，评审意见已并入 §7）。
> 本文件是 AGENTS.md / DESIGN.md 瘦身与 docs/ 重组的唯一计划源。
> 执行完毕后本文件移入 `docs/archive/`，不再留在根目录。
> 注：DESIGN.md 在仓库根目录（非 docs/ 下）；另有 CLAUDE.md / LESSONS.md / FENGMEM.md / README.md
> 与 AGENTS 部分重复，本次先不动它们，只修会被本次改动打断的锚点与路径。

## 1. 背景

- `AGENTS.md` 639 行、`DESIGN.md` 1484 行，两边重复讲白色横线、内容卡片、chips、暗色、响应式，新人/新 AI 要读两遍。
- `docs/` 根下 20+ 裸文件，一次性历史（AUDIT/FIX/REPORT/PLAN/STRATEGY/HANDOFF）与常查手册混放，越积越重。
- 上一版计划的问题：要在 `docs/guide/` 下新建 6 篇并从 DESIGN.md 摘出第三份拷贝，会越整越多；且未明确什么必须留在 AGENTS 正文、什么可以藏链接后。

## 2. 划分标准（根本性 vs 细节）

判断方法只有一条：**违反之后能不能低成本改回来。**

- 根本性（必须每个 AI 开工就看到，留在 AGENTS 正文，预计 80–100 行）：
  1. 一句话项目定位 + 架构铁律（纯静态 0 运行时，仅 Cloudflare Pages；AI 走 BYOK；出处 `docs/decisions/DECISION-0001-zero-runtime-byok.md`）。
  2. 目录与两条常用命令（博客构建 `hugo/deploy.sh`、快速检查 `check_site.py --no-dark` vs 发版前全量）。
  3. 分支与门禁一句话：日常 dev 只跑快速检查；合并推送 master 前才跑全量 + 用户批准。
  4. 安全红线（现有 5 个 NEVER 原样保留：不经批准不 push、不推 master、不直接 commit 到 master、不删 assets、不提交 .env）。
  5. 一张任务→文档索引表（写文章去哪、改页面结构去哪、改样式暗色去 DESIGN.md 哪节、发版流程去哪、踩坑去哪）。
- 细节（从 AGENTS 正文删除，只留链接，遇到具体任务再读）：思想锚点表、白色横线原理、卡片/chips/QA 代码写法、翻译与 punchline 细节、踩坑 11 条全文。

## 3. docs/ 重组：根下只留文件夹 + 一张地图

重组后 `docs/` 根下只允许一个文件 `README.md`（全站文档地图），其余全是文件夹：

- `docs/decisions/`：架构决策，永久有效。`DECISION-0001-zero-runtime-byok.md` 搬入；以后 DECISION-0002 起依次加。这是铁律出处。
- `docs/guide/`：常查手册，只放稳定细节，不放一次性记录，共 5 篇，全部从现有文字搬运、不改写：
  1. 语音与翻译（含 punchline、简繁规则；AGENTS 相关节搬入）；
  2. 页面结构与跳转（卡片网格、cross-link、文件名规范）；
  3. 发版与门禁全文（含 Git 流程、Docker 预览、Privacy）；
  4. 踩坑记录（AGENTS 现有 11 条原样搬）；
  5. 博文写作规范（现有 `WRITING-博文写作规范.md` 直接 `git mv` 移入，不复制第二份）。
- `docs/archive/`：一次性历史，只读归档。AUDIT、FIX、REPORT、REVIEW、PLAN 系列、STRATEGY 系列、HANDOFF、migrate、rename、2026-08 计划、暗色审计 txt 全部移入。日常不读，出问题追溯再翻。
- `docs/briefs/`、`docs/notes/`：保持不动。

## 4. DESIGN.md 处理（修正上一版）

- DESIGN.md 保持为视觉参数唯一源头，不动大段。
- AGENTS 与 `guide/` 里凡讲白色横线、内容块、chips、暗色、响应式处，只写一句话 + 指向 DESIGN.md 对应章节的链接。
- 反向由 DESIGN.md 加一句指向 `guide/` 任务入口。去重只删 AGENTS 一侧。
- 年份问题：2021/2022 改名已在上次提交做完，DESIGN.md 自定义部分无残留，剩余为第三方 min 库，不动。

## 5. 执行顺序

1. 建文件夹并用 `git mv` 搬运原文件（不改写、不合并）。
2. 写 `docs/README.md` 地图。
3. AGENTS.md 重写为瘦版 + 索引。
4. DESIGN.md 加双向指向。
5. 跑 `python3 tools/check_site.py --no-dark` 验证文档链接无死链。
6. 全程只动文档不动页面，另起一个 commit，不推送；发版到 master 前再跑全量审计。

## 6. 待独立评审的问题（已评审，结论见 §7）

- 根本性划分是否过严/过松？→ 评审结论：过严，6 条压缩不变量必须留正文。
- `guide/` 5 篇划分是否合理？→ 合理，另有两处修正（见 §7.3）。
- `docs/` 根只留 README 是否影响现有脚本与外链？→ 有 5 处真实断裂，随改随修（见 §7.4）。
- 是否需要给 `archive/` 加只读声明？→ 需要，加 `docs/archive/README.md` 一句话。

## 7. 独立评审意见（approve-with-changes，执行前必须满足）

### 7.1 AGENTS 正文目标放宽到 120–150 行，6 条压缩不变量必须留

每条只留 1–3 行，长代码示例进 `guide/`：文件名规范（改名断 URL，不可逆）、
白色横线结构不变量（content-block 须为 page-wrap 直接子元素）、暗色无全局兜底原则、
cross-link 链、h2=punchline 与 NO second-person / NO self-praise、storm-bg 仅 Tech 页禁令。

### 7.2 踩坑记录不照搬 11 条全文

拆成"压缩不变量留 AGENTS + 详细修复法进 guide"，避免与发版门禁 doc 形成"何时跑全量"的第二出处；
条目重编号时同步更新引用。

### 7.3 guide/archive 归属修正

- `CHECKLIST-全站检测清单.md` → 归档（其头部已声明机械检查并入 `tools/check_site.py`）。
- `INTERVIEW-hard-questions.md`（标 待答，活文档）→ 不进 `archive/`，暂留根目录或移 `notes/`。

### 7.4 同一次 commit 必须随手修的断裂（已按新意见调整）

1. `AGENTS.md:38` 的 `docs/DECISION-0001-*.md` 路径 → `docs/decisions/` 新路径。
2. ~~`FENGMEM.md` 旧路径引用~~ → **不修**。FENGMEM 为冻结存档，一字不动；
   其中旧 `docs/` 路径失效是预期的，在 `docs/README.md` 地图里加一条注释说明即可。
3. `docs/PLAN-防复发机制-2026-09-06.md:42` 对 CHECKLIST 的引用 → 新路径（该文件本身进 archive，引用随之改）。
4. `docs/2026-08-new-projects-plan.md:87` 引用的 `docs/email-classification.md` 本就不存在 → 标注，不传播。
5. `README.md` 与 `CLAUDE.md` 锚到 AGENTS 章节的问题 → 随两文件本身的重写一次性解决（见 §7.6）。
6. `tools/check_site.py:4` 注释里点的 `docs/AUDIT-…` 旧名 → 更新注释（脚本本身无文档链接检查逻辑）。

### 7.6 根目录文档处理（新增：CLAUDE 指针化、FENGMEM 冻结、README 同步瘦身）

- `CLAUDE.md`（70 行）→ 瘦成指针文件（约 10 行）：只写一句话
  "本仓库唯一规范是 `AGENTS.md`（行为）+ `DESIGN.md`（视觉参数），开工前必读这两份，
  本文件不重复任何规则"，附 3 个链接（AGENTS 门禁节、DESIGN.md、docs/guide 索引）。
  删除暗色代码块、固定规则、检查脚本复述；检查口径随重写统一为
  dev 跑 `--no-dark`、合 master 前跑全量 + 用户批准（顺手解决 AGENTS/CLAUDE 矛盾）。
- `FENGMEM.md` → 冻结存档，一字不动，不更新旧路径，不加文件头。
  失效引用由 `docs/README.md` 地图统一注释。
- `README.md`（324 行）→ 与 AGENTS 同步瘦身：保留项目定位、Quick Start、目录结构；
  删除重复的 White Divider / Content Block / Chips / QA / Dark Mode / Responsive 代码段，
  改为指向 AGENTS + DESIGN.md 对应章节；Key Rules / Design Principles / Git Strategy
  长段压缩为 2–3 行 + 链接；Docker Preview 与 Secrets 段搬 `guide/` 发版篇，README 留链接。
  注意 README 是 GitHub 门面，保留人类可读性，不写成纯跳转页。
- `LESSONS.md`（179 行）→ 教训正文并入 `guide/` 踩坑篇（详细），审查清单并入检查脚本或归档；
  根 `LESSONS.md` 改为指针指向 `guide/` + AGENTS，消灭第五份重复源。
  其中"master 领先 dev"事故的主分支纪律已在 AGENTS 安全红线内，合并时去重。

### 7.5 执行顺序修正

- `check_site.py --no-dark` 不查 markdown 死链，第 5 步换成真实文档链接检查（grep 锚点/路径，或新增检查节）。
- 开工前 `git status` 干净；分两次提交：纯 `git mv` 一次、内容改写一次，保证 `git log --follow` 可用。
- AGENTS/CLAUDE 检查口径矛盾（CLAUDE 令 push 前全量 vs AGENTS 令 dev 只跑快速）→ 瘦版索引表必须统一为：
  日常 dev 跑 `--no-dark`，合并推送 master 前跑全量 + 用户批准。
