# CLAUDE — 指针文件（本仓库唯一规范是 AGENTS.md + DESIGN.md）

开工前必读 `AGENTS.md`（行为：分支、安全、门禁、不变量）+ `DESIGN.md`（视觉参数唯一源头）。
本文件不重复任何规则。

- 门禁口径：日常 dev 跑 `python3 tools/check_site.py --no-dark`；合并推送 master 前跑全量 + 用户明确批准。全文见 `docs/guide/release-gate.md`。
- 任务索引见 `AGENTS.md` 末表；文档地图见 `docs/README.md`。
